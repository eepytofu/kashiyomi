[
  {
    "id": "kana/kata-to-hira-offset",
    "file": "src/engine/kana.ts",
    "what": "katakana->hiragana offset wrong",
    "find": "const KATA_TO_HIRA_OFFSET = 0x60;",
    "replace": "const KATA_TO_HIRA_OFFSET = 0x61;"
  },
  {
    "id": "kana/kata-to-hira-identity",
    "file": "src/engine/kana.ts",
    "what": "kataToHira never converts",
    "find": "      ? String.fromCodePoint(code - KATA_TO_HIRA_OFFSET)",
    "replace": "      ? ch"
  },
  {
    "id": "kana/katakana-end-short",
    "file": "src/engine/kana.ts",
    "what": "conversion range one short (ヶ/ヵ edge)",
    "find": "const KATAKANA_END = 0x30f6;",
    "replace": "const KATAKANA_END = 0x30f5;"
  },
  {
    "id": "kana/okurigana-drop-hiragana-guard",
    "file": "src/engine/kana.ts",
    "what": "katakana-okurigana no longer requires the line to lack hiragana",
    "find": "  return HAN_CHAR.test(text) && KATAKANA_CHAR.test(text) && !HIRAGANA_CHAR.test(text);",
    "replace": "  return HAN_CHAR.test(text) && KATAKANA_CHAR.test(text);"
  },
  {
    "id": "kana/okurigana-drop-han-guard",
    "file": "src/engine/kana.ts",
    "what": "katakana-okurigana no longer requires kanji",
    "find": "  return HAN_CHAR.test(text) && KATAKANA_CHAR.test(text) && !HIRAGANA_CHAR.test(text);",
    "replace": "  return KATAKANA_CHAR.test(text) && !HIRAGANA_CHAR.test(text);"
  },
  {
    "id": "kana/kana-char-drop-katakana",
    "file": "src/engine/kana.ts",
    "what": "KANA_CHAR no longer matches katakana",
    "find": "export const KANA_CHAR = /[ぁ-ゖァ-ヺーゝゞヽヾ]/u;",
    "replace": "export const KANA_CHAR = /[ぁ-ゖ]/u;"
  },
  {
    "id": "kana/kana-char-drop-prolong",
    "file": "src/engine/kana.ts",
    "what": "KANA_CHAR no longer matches ー and iteration marks",
    "find": "export const KANA_CHAR = /[ぁ-ゖァ-ヺーゝゞヽヾ]/u;",
    "replace": "export const KANA_CHAR = /[ぁ-ゖァ-ヺ]/u;"
  },
  {
    "id": "kana/han-char-drop-marks",
    "file": "src/engine/kana.ts",
    "what": "HAN_CHAR drops 々〆ヵヶ",
    "find": "export const HAN_CHAR = /[\\p{Script=Han}々〆ヵヶ]/u;",
    "replace": "export const HAN_CHAR = /[\\p{Script=Han}]/u;"
  },
  {
    "id": "kana/kana-only-drop-prolong",
    "file": "src/engine/kana.ts",
    "what": "KANA_ONLY rejects ー and iteration marks",
    "find": "export const KANA_ONLY = /^[ぁ-ゖァ-ヺーゝゞヽヾ]+$/u;",
    "replace": "export const KANA_ONLY = /^[ぁ-ゖァ-ヺ]+$/u;"
  },
  {
    "id": "hints/drop-okurigana-check",
    "file": "src/engine/hints.ts",
    "what": "reading no longer has to end with the word's okurigana",
    "find": "    if (okurigana !== \"\" && !kataToHira(reading).endsWith(kataToHira(okurigana))) {",
    "replace": "    if (false && okurigana !== \"\" && !kataToHira(reading).endsWith(kataToHira(okurigana))) {"
  },
  {
    "id": "hints/okurigana-check-no-normalize",
    "file": "src/engine/hints.ts",
    "what": "okurigana compared without katakana normalization",
    "find": "    if (okurigana !== \"\" && !kataToHira(reading).endsWith(kataToHira(okurigana))) {",
    "replace": "    if (okurigana !== \"\" && !reading.endsWith(okurigana)) {"
  },
  {
    "id": "hints/drop-chant-rejection",
    "file": "src/engine/hints.ts",
    "what": "chant-echo parentheticals accepted as readings",
    "find": "    if (DECORATION_ONLY.test(tail) && isChantContext(line, index, word)) {",
    "replace": "    if (false && DECORATION_ONLY.test(tail) && isChantContext(line, index, word)) {"
  },
  {
    "id": "hints/chant-drop-tail-check",
    "file": "src/engine/hints.ts",
    "what": "chant rejection no longer requires the paren to end the line",
    "find": "    if (DECORATION_ONLY.test(tail) && isChantContext(line, index, word)) {",
    "replace": "    if (isChantContext(line, index, word)) {"
  },
  {
    "id": "hints/chant-word-length",
    "file": "src/engine/hints.ts",
    "what": "chant rejection drops the word-length requirement",
    "find": "  return KANA_ONLY_LOOSE.test(before) && word.length >= 2;",
    "replace": "  return KANA_ONLY_LOOSE.test(before);"
  },
  {
    "id": "hints/chant-drop-kana-context",
    "file": "src/engine/hints.ts",
    "what": "chant rejection ignores what precedes the word",
    "find": "  return KANA_ONLY_LOOSE.test(before) && word.length >= 2;",
    "replace": "  return word.length >= 2;"
  },
  {
    "id": "hints/pattern-allow-space",
    "file": "src/engine/hints.ts",
    "what": "a spaced-off aside now counts as a reading hint",
    "find": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,8})[（(]([ぁ-ゖァ-ヺー]{1,24})[）)]/gu;",
    "replace": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,8})\\s*[（(]([ぁ-ゖァ-ヺー]{1,24})[）)]/gu;"
  },
  {
    "id": "hints/pattern-allow-nonkana-reading",
    "file": "src/engine/hints.ts",
    "what": "parenthetical no longer has to be kana",
    "find": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,8})[（(]([ぁ-ゖァ-ヺー]{1,24})[）)]/gu;",
    "replace": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,8})[（(]([^）)]{1,24})[）)]/gu;"
  },
  {
    "id": "hints/pattern-drop-okurigana-capture",
    "file": "src/engine/hints.ts",
    "what": "okurigana never captured as part of the word",
    "find": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,8})[（(]([ぁ-ゖァ-ヺー]{1,24})[）)]/gu;",
    "replace": "  /([\\p{Script=Han}々〆ヵヶ]{1,12})([ぁ-ゖー]{0,0})[（(]([ぁ-ゖァ-ヺー]{1,24})[）)]/gu;"
  },
  {
    "id": "metadata/drop-lowercase",
    "file": "src/engine/metadata.ts",
    "what": "credit labels matched case-sensitively",
    "find": "  return label.replace(/[\\s·・]/gu, \"\").toLowerCase();",
    "replace": "  return label.replace(/[\\s·・]/gu, \"\");"
  },
  {
    "id": "metadata/drop-space-strip",
    "file": "src/engine/metadata.ts",
    "what": "credit labels not stripped of spaces and interpuncts",
    "find": "  return label.replace(/[\\s·・]/gu, \"\").toLowerCase();",
    "replace": "  return label.toLowerCase();"
  },
  {
    "id": "metadata/compound-any-part",
    "file": "src/engine/metadata.ts",
    "what": "compound label accepted when only some parts are credits",
    "find": "  return parts.every((part) => roleCoverRatio(part) >= ROLE_COVER_MIN);",
    "replace": "  return parts.some((part) => roleCoverRatio(part) >= ROLE_COVER_MIN);"
  },
  {
    "id": "metadata/cover-threshold-halved",
    "file": "src/engine/metadata.ts",
    "what": "a label only a third covered by role morphemes counts as a credit",
    "find": "const ROLE_COVER_MIN = 2 / 3;",
    "replace": "const ROLE_COVER_MIN = 1 / 3;"
  },
  {
    "id": "metadata/cover-threshold-exclusive",
    "file": "src/engine/metadata.ts",
    "what": "a label covered exactly to the threshold is rejected, dropping 录音室",
    "find": "  return parts.every((part) => roleCoverRatio(part) >= ROLE_COVER_MIN);",
    "replace": "  return parts.every((part) => roleCoverRatio(part) > ROLE_COVER_MIN);"
  },
  {
    "id": "metadata/cover-without-skip",
    "file": "src/engine/metadata.ts",
    "what": "cover must be contiguous, so an uncoverable character fails the whole label as it did before",
    "find": "    if (best[start]! > best[start + 1]!) best[start + 1] = best[start]!;",
    "replace": ""
  },
  {
    "id": "metadata/allow-empty-value",
    "file": "src/engine/metadata.ts",
    "what": "credit line no longer needs a value after the colon",
    "find": "  /^\\s*(?:\\d{1,3}\\s*[.．、)]\\s*)?([\\p{L}\\p{N}][\\p{L}\\p{N}\\s&/・·,，、-]{0,40}?)\\s*[:：]\\s*(\\S.*)$/u;",
    "replace": "  /^\\s*(?:\\d{1,3}\\s*[.．、)]\\s*)?([\\p{L}\\p{N}][\\p{L}\\p{N}\\s&/・·,，、-]{0,40}?)\\s*[:：]\\s*(.*)$/u;"
  },
  {
    "id": "metadata/label-length-unbounded",
    "file": "src/engine/metadata.ts",
    "what": "credit label length bound removed",
    "find": "[\\p{L}\\p{N}\\s&/・·,，、-]{0,40}?)",
    "replace": "[\\p{L}\\p{N}\\s&/・·,，、-]{0,400}?)"
  },
  {
    "id": "hanRepair/drop-length-guard",
    "file": "src/engine/hanRepair.ts",
    "what": "phrase-level OpenCC rewrites no longer rejected",
    "find": "  if (converted.length !== line.length) {",
    "replace": "  if (false && converted.length !== line.length) {"
  },
  {
    "id": "hanRepair/drop-kanau-restore",
    "file": "src/engine/hanRepair.ts",
    "what": "叶う no longer restored from 葉う",
    "find": "  return repaired.replace(/葉([いうえわお])/gu, \"叶$1\");",
    "replace": "  return repaired;"
  },
  {
    "id": "hanRepair/kanau-restore-always",
    "file": "src/engine/hanRepair.ts",
    "what": "葉 restored to 叶 regardless of okurigana",
    "find": "  return repaired.replace(/葉([いうえわお])/gu, \"叶$1\");",
    "replace": "  return repaired.replace(/葉(.?)/gu, \"叶$1\");"
  },
  {
    "id": "furigana/drop-kana-only-guard",
    "file": "src/engine/furigana.ts",
    "what": "non-kana readings no longer rejected",
    "find": "  if (surface === \"\" || readingKana === \"\" || !KANA_ONLY.test(readingKana)) return [];",
    "replace": "  if (surface === \"\" || readingKana === \"\") return [];"
  },
  {
    "id": "furigana/drop-ambiguity-abstain",
    "file": "src/engine/furigana.ts",
    "what": "lazy/greedy disagreement no longer abstains (guessed splits)",
    "find": "  if (!greedy || lazy.join(\"\\u0000\") !== greedy.join(\"\\u0000\")) {",
    "replace": "  if (!greedy) {"
  },
  {
    "id": "furigana/drop-kana-anchor-normalize",
    "file": "src/engine/furigana.ts",
    "what": "kana anchors not normalized before matching",
    "find": "    pattern += run.isHan ? quant : escapeRegex(kataToHira(run.text));",
    "replace": "    pattern += run.isHan ? quant : escapeRegex(run.text);"
  },
  {
    "id": "furigana/no-han-returns-whole",
    "file": "src/engine/furigana.ts",
    "what": "kana-only surfaces get ruby instead of none",
    "find": "  if (hanRuns.length === 0) return [];",
    "replace": "  if (hanRuns.length === 0) return [{ start: 0, end: surface.length, reading: kataToHira(readingKana) }];"
  },
  {
    "id": "japanese/drop-sokuon-carry",
    "file": "src/engine/japanese.ts",
    "what": "token-final っ no longer geminates across the boundary",
    "find": "    if (!isFinal && hira.endsWith(\"っ\")) {",
    "replace": "    if (false && !isFinal && hira.endsWith(\"っ\")) {"
  },
  {
    "id": "japanese/sokuon-carry-when-final",
    "file": "src/engine/japanese.ts",
    "what": "sokuon carried off the end of the line too",
    "find": "    if (!isFinal && hira.endsWith(\"っ\")) {",
    "replace": "    if (hira.endsWith(\"っ\")) {"
  },
  {
    "id": "japanese/drop-particle-wa",
    "file": "src/engine/japanese.ts",
    "what": "particle は voiced as ha",
    "find": "  if (surface === \"は\") return \"wa\";",
    "replace": "  if (false) return \"wa\";"
  },
  {
    "id": "japanese/drop-particle-pos-check",
    "file": "src/engine/japanese.ts",
    "what": "any は becomes wa, not just the particle",
    "find": "  if (token.partOfSpeech !== \"particle\") return undefined;",
    "replace": "  if (false) return undefined;"
  },
  {
    "id": "japanese/hint-overlap-inclusive",
    "file": "src/engine/japanese.ts",
    "what": "hint/token overlap test made inclusive at the edges",
    "find": "    const hint = hints.find((h) => h.start < token.end && h.end > token.start);",
    "replace": "    const hint = hints.find((h) => h.start <= token.end && h.end >= token.start);"
  },
  {
    "id": "japanese/drop-hint-run-grouping",
    "file": "src/engine/japanese.ts",
    "what": "a hint spanning several tokens no longer groups them",
    "find": "    while (last + 1 < tokens.length && tokens[last + 1]!.start < hint.end) last += 1;",
    "replace": "    while (false) last += 1;"
  },
  {
    "id": "japanese/drop-context-kana-check",
    "file": "src/engine/japanese.ts",
    "what": "non-kana context around a hint is voiced anyway",
    "find": "    const voicedKana = prefixOk && suffixOk ? prefix + hint.reading + suffix : hint.reading;",
    "replace": "    const voicedKana = prefix + hint.reading + suffix;"
  },
  {
    "id": "japanese/drop-authored-alignment",
    "file": "src/engine/japanese.ts",
    "what": "authored hints never anchored on okurigana",
    "find": "    const authored = alignFurigana(hintSurface, hint.reading);",
    "replace": "    const authored = [];"
  },
  {
    "id": "japanese/drop-no-space-after",
    "file": "src/engine/japanese.ts",
    "what": "romaji spacing ignores trailing punctuation",
    "find": "    NO_SPACE_AFTER.test(parts[parts.length - 1]?.text ?? \"\");",
    "replace": "    false;"
  },
  {
    "id": "japanese/drop-empty-romaji-guard",
    "file": "src/engine/japanese.ts",
    "what": "empty romaji parts appended",
    "find": "  if (romaji === \"\") return;",
    "replace": "  if (false) return;"
  },
  {
    "id": "japanese/furigana-sorted-descending",
    "file": "src/engine/japanese.ts",
    "what": "furigana segments ordered back to front",
    "find": "  furigana.sort((a, b) => a.start - b.start);",
    "replace": "  furigana.sort((a, b) => b.start - a.start);"
  },
  {
    "id": "japanese/surface-fallback-removed",
    "file": "src/engine/japanese.ts",
    "what": "tokens with no reading voice as empty instead of their surface",
    "find": "        const kana = token.readingKana !== \"\" ? token.readingKana : token.surface;",
    "replace": "        const kana = token.readingKana;"
  },
  {
    "id": "lineKinds/drop-marker-check",
    "file": "src/engine/lineKinds.ts",
    "what": "singer markers fall through to the credit tests",
    "find": "    if (isPartMarkerLine(line.text)) return \"marker\";",
    "replace": "    if (false && isPartMarkerLine(line.text)) return \"marker\";"
  },
  {
    "id": "lineKinds/scan-bound-unbounded",
    "file": "src/engine/lineKinds.ts",
    "what": "the credit-shape fallback may look anywhere in the song",
    "find": "export const CREDIT_SCAN_LINES = 6;",
    "replace": "export const CREDIT_SCAN_LINES = 600;"
  },
  {
    "id": "lineKinds/drop-translation-gate",
    "file": "src/engine/lineKinds.ts",
    "what": "shape fallback fires in a song that carries no translations at all",
    "find": "      index < CREDIT_SCAN_LINES &&\n      songHasTranslations &&",
    "replace": "      index < CREDIT_SCAN_LINES &&"
  },
  {
    "id": "lineKinds/shape-accepts-translated",
    "file": "src/engine/lineKinds.ts",
    "what": "a translated opening line counts as a credit",
    "find": "      line.translation === \"untranslated\" &&",
    "replace": "      line.translation !== \"untranslated\" &&"
  },
  {
    "id": "lineKinds/drop-credit-run",
    "file": "src/engine/lineKinds.ts",
    "what": "credit blocks are left to the translation-gated fallback alone",
    "find": "    if (inCreditRun[index] && hasCreditShape(line.text)) return \"credit\";",
    "replace": "    if (false && inCreditRun[index] && hasCreditShape(line.text)) return \"credit\";"
  },
  {
    "id": "lineKinds/run-unanchored",
    "file": "src/engine/lineKinds.ts",
    "what": "a credit-shaped line opens a run with no listed credit to anchor it",
    "find": "    if (!isCreditLine(lines[i]!.text)) continue;",
    "replace": "    if (!continues[i]) continue;"
  },
  {
    "id": "lineKinds/run-never-breaks",
    "file": "src/engine/lineKinds.ts",
    "what": "a run swallows the sung lines instead of stopping at the first one",
    "find": "    for (let j = i; j < lines.length && continues[j]; j++) inRun[j] = true;",
    "replace": "    for (let j = i; j < lines.length; j++) inRun[j] = true;"
  },
  {
    "id": "lineKinds/run-grows-forward-only",
    "file": "src/engine/lineKinds.ts",
    "what": "a credit block is only ever found by growing down, so a block at the foot of the song is missed",
    "find": "    for (let j = i; j >= 0 && continues[j]; j--) inRun[j] = true;",
    "replace": ""
  },
  {
    "id": "lineKinds/doc-translations-every",
    "file": "src/engine/lineKinds.ts",
    "what": "a song counts as translated only when every line is",
    "find": "  const songHasTranslations = lines.some((line) => line.translation === \"translated\");",
    "replace": "  const songHasTranslations = lines.every((line) => line.translation === \"translated\");"
  },
  {
    "id": "lineText/hints-off-still-strips",
    "file": "src/engine/lineText.ts",
    "what": "hints off rewrites the display instead of leaving NetEase's line alone",
    "find": "  const displayText = options.readingHints ? projection.displayText : shown;",
    "replace": "  const displayText = projection.displayText;"
  },
  {
    "id": "lineText/analyzer-sees-brackets",
    "file": "src/engine/lineText.ts",
    "what": "with hints off the analyzer is handed the bracketed line",
    "find": "  const unbracketed = options.readingHints ? analysisShown : maskHintBrackets(analysisShown);",
    "replace": "  const unbracketed = analysisShown;"
  },
  {
    "id": "lineText/okurigana-unconditional",
    "file": "src/engine/lineText.ts",
    "what": "every line is converted to hiragana, not just okurigana lines",
    "find": "  const analysisText = usesKatakanaOkurigana(unbracketed)\n    ? kataToHira(unbracketed)\n    : unbracketed;",
    "replace": "  const analysisText = kataToHira(unbracketed);"
  },
  {
    "id": "lineText/drop-han-repair",
    "file": "src/engine/lineText.ts",
    "what": "the kanji repair setting stops affecting the display, so repair never appears to run",
    "find": "  const shown = options.hanRepair ? repaired : source;",
    "replace": "  const shown = source;"
  },
  {
    "id": "lineText/repair-gates-analysis",
    "file": "src/engine/lineText.ts",
    "what": "analysis follows the display setting again, so repair off means OOV and no furigana",
    "find": "  const analysisShown = options.readingHints ? analysed.displayText : repaired;",
    "replace": "  const analysisShown = options.readingHints ? projection.displayText : shown;"
  },
  {
    "id": "hanForms/drop-iteration-mark",
    "file": "src/engine/hanForms.ts",
    "what": "々 no longer counts as japanese orthography",
    "find": "    if (ch === ITERATION_MARK || JAPANESE_ONLY_KANJI.has(ch)) return true;",
    "replace": "    if (JAPANESE_ONLY_KANJI.has(ch)) return true;"
  },
  {
    "id": "hanForms/drop-kokuji",
    "file": "src/engine/hanForms.ts",
    "what": "kokuji dropped from the japanese-only set",
    "find": "  ...(\"働込峠畑辻匂凪雫枠榊麿躾塀笹咲栃搾腺\"),",
    "replace": "  ...(\"\"),"
  },
  {
    "id": "hanForms/chinese-only-inverted",
    "file": "src/engine/hanForms.ts",
    "what": "chinese-only glyph detection inverted",
    "find": "  return toJapaneseGlyphs(line) !== line;",
    "replace": "  return toJapaneseGlyphs(line) === line;"
  },
  {
    "id": "latin/drop-ideographic-space",
    "file": "src/engine/latinPunctuation.ts",
    "what": "U+3000 survives into a romanized row",
    "find": "  [\"　\", \" \"],",
    "replace": "  [\"　\", \"　\"],"
  },
  {
    "id": "latin/drop-enumeration-comma",
    "file": "src/engine/latinPunctuation.ts",
    "what": "、 is left as-is instead of becoming a comma",
    "find": "  [\"、\", \",\"],",
    "replace": "  [\"、\", \"、\"],"
  },
  {
    "id": "latin/drop-middle-dot",
    "file": "src/engine/latinPunctuation.ts",
    "what": "the name-separating middle dot is not turned into a space",
    "find": "  [\"・\", \" \"],",
    "replace": "  [\"・\", \"・\"],"
  },
  {
    "id": "latin/no-space-after-mark",
    "file": "src/engine/latinPunctuation.ts",
    "what": "no space is inserted after a mark that needs one",
    "find": "    .replace(/([,.!?;:])(?=[^\\s,.!?;:])/gu, \"$1 \")",
    "replace": "    .replace(/([,.!?;:])(?=[^\\s,.!?;:])/gu, \"$1\")"
  },
  {
    "id": "latin/keep-space-before-mark",
    "file": "src/engine/latinPunctuation.ts",
    "what": "a space is left in front of a closing mark",
    "find": "    .replace(/[ \\t]+([,.!?;:)\\]\"'])/gu, \"$1\")",
    "replace": "    .replace(/[ \\t]+([,.!?;:)\\]\"'])/gu, \" $1\")"
  },
  {
    "id": "latin/double-space-across-segments",
    "file": "src/engine/latinPunctuation.ts",
    "what": "a space on each side of a segment boundary stays a double space",
    "find": "    } else if (last === \" \" || last === \"\\t\") {",
    "replace": "    } else if (false) {"
  },
  {
    "id": "apiKeys/no-comma-separator",
    "file": "src/engine/apiKeys.ts",
    "what": "only newlines separate keys, so a comma-pasted list is one key",
    "find": "    .split(/[\\n,]+/u)",
    "replace": "    .split(/[\\n]+/u)"
  },
  {
    "id": "apiKeys/drop-resting-keys",
    "file": "src/engine/apiKeys.ts",
    "what": "keys still cooling down are dropped instead of tried last",
    "find": "  return [...ready, ...resting];",
    "replace": "  return ready;"
  },
  {
    "id": "apiKeys/no-rotation",
    "file": "src/engine/apiKeys.ts",
    "what": "rotation ignores the last key that worked",
    "find": "    rotated.push(keys[(startIndex + i) % keys.length]!);",
    "replace": "    rotated.push(keys[i]!);"
  },
  {
    "id": "apiKeys/drop-401",
    "file": "src/engine/apiKeys.ts",
    "what": "a rejected key is not treated as exhausted",
    "find": "  return status === 429 || status === 402 || status === 401 || status === 403;",
    "replace": "  return status === 429 || status === 402 || status === 403;"
  },
  {
    "id": "apiKeys/cooldown-boundary",
    "file": "src/engine/apiKeys.ts",
    "what": "a key whose cooldown just expired is still treated as resting",
    "find": "  const ready = rotated.filter((key) => (cooldown.get(key) ?? 0) <= now);",
    "replace": "  const ready = rotated.filter((key) => (cooldown.get(key) ?? 0) < now);"
  },
  {
    "id": "tokens/drop-overlap-check",
    "file": "src/engine/tokens.ts",
    "what": "overlapping and out-of-bounds token ranges are accepted",
    "find": "    if (token.start < previousEnd || token.end < token.start || token.end > text.length) {",
    "replace": "    if (token.end < token.start) {"
  },
  {
    "id": "tokens/drop-surface-check",
    "file": "src/engine/tokens.ts",
    "what": "a token whose range does not hold its surface is accepted",
    "find": "    if (text.slice(token.start, token.end) !== token.surface) {",
    "replace": "    if (false) {"
  },
  {
    "id": "tokens/drop-integer-check",
    "file": "src/engine/tokens.ts",
    "what": "non-integer offsets are accepted",
    "find": "    if (!Number.isInteger(token.start) || !Number.isInteger(token.end)) {",
    "replace": "    if (false) {"
  },
  {
    "id": "romaji/drop-particle-wa",
    "file": "src/engine/romaji.ts",
    "what": "the topic particle は romanizes as ha",
    "find": "    if (s === \"は\") return \"wa\";",
    "replace": "    if (s === \"はは\") return \"wa\";"
  },
  {
    "id": "romaji/drop-particle-he",
    "file": "src/engine/romaji.ts",
    "what": "the particle へ romanizes as he",
    "find": "    if (s === \"へ\") return \"e\";",
    "replace": "    if (s === \"へへ\") return \"e\";"
  },
  {
    "id": "romaji/drop-n-apostrophe",
    "file": "src/engine/romaji.ts",
    "what": "ん before a vowel loses its disambiguating apostrophe",
    "find": "      out += nextStart && (VOWELS.has(nextStart) || nextStart === \"y\") ? \"n'\" : \"n\";",
    "replace": "      out += \"n\";"
  },
  {
    "id": "romaji/surface-fallback-dropped",
    "file": "src/engine/romaji.ts",
    "what": "a token with no reading romanizes as empty instead of its surface",
    "find": "  return kanaToRomaji(readingKana !== \"\" ? readingKana : surface);",
    "replace": "  return kanaToRomaji(readingKana);"
  },
  {
    "id": "pinyin/no-tone-sandhi",
    "file": "src/engine/pinyin.ts",
    "what": "一 and 不 keep their plain tone instead of the written change",
    "find": "    toneSandhi: true,",
    "replace": "    toneSandhi: false,"
  },
  {
    "id": "heteronyms/watashi-stays-formal",
    "file": "src/engine/japaneseHeteronyms.ts",
    "what": "私 keeps sudachi's ワタクシ, so a first-person lyric reads in humble register",
    "find": "  if (token.surface === \"私\" && token.readingKana !== WATASHI) return WATASHI;",
    "replace": "  if (false) return WATASHI;"
  },
  {
    "id": "heteronyms/nani-never-corrected",
    "file": "src/engine/japaneseHeteronyms.ts",
    "what": "何 keeps ナン everywhere, so 何も reads nan mo",
    "find": "  if (token.surface !== \"何\" || token.readingKana !== NAN) return undefined;",
    "replace": "  if (true) return undefined;"
  },
  {
    "id": "heteronyms/nani-ignores-assimilation",
    "file": "src/engine/japaneseHeteronyms.ts",
    "what": "何 before a coronal is read なに, so 何だ becomes nani da",
    "find": "  if (HAN.test(following) || CORONAL.has(following)) return undefined;",
    "replace": "  if (HAN.test(following)) return undefined;"
  },
  {
    "id": "heteronyms/nani-before-kanji",
    "file": "src/engine/japaneseHeteronyms.ts",
    "what": "何 before kanji is read なに, so an unabsorbed 何人 becomes nani nin",
    "find": "  if (HAN.test(following) || CORONAL.has(following)) return undefined;",
    "replace": "  if (CORONAL.has(following)) return undefined;"
  },
  {
    "id": "heteronyms/bare-nani-stays-nan",
    "file": "src/engine/japaneseHeteronyms.ts",
    "what": "a line-final 何 keeps ナン, though a bare interrogative 何 is なに",
    "find": "  if (following === \"\") return NANI;",
    "replace": "  if (following === \"\") return undefined;"
  },
  {
    "id": "dictionary/accepts-sdist",
    "file": "src/engine/dictionarySource.ts",
    "what": "an sdist stub is accepted as a source, routing the download back to plaintext http",
    "find": "    if (file.packagetype !== \"bdist_wheel\") continue;",
    "replace": "    if (false) continue;"
  },
  {
    "id": "dictionary/mirror-picks-arbitrary-version",
    "file": "src/engine/dictionarySource.ts",
    "what": "the mirror index takes whichever release is listed last instead of the newest",
    "find": "    if (release && (best === undefined || release.version > best.version)) best = release;",
    "replace": "    if (release) best = release;"
  },
  {
    "id": "dictionary/installs-without-a-hash",
    "file": "src/engine/dictionarySource.ts",
    "what": "a release with no usable checksum is accepted, so unverifiable bytes can install",
    "find": "  if (typeof sha256 !== \"string\" || !/^[0-9a-f]{64}$/u.test(sha256)) return undefined;",
    "replace": "  if (typeof sha256 !== \"string\") return undefined;"
  },
  {
    "id": "dictionary/checksum-mismatch-installs",
    "file": "src/engine/dictionarySource.ts",
    "what": "a corrupt or tampered download is installed anyway",
    "find": "  return actualSha256.toLowerCase() === expected.sha256.toLowerCase();",
    "replace": "  return true;"
  },
  {
    "id": "dictionary/free-space-ignores-extraction",
    "file": "src/engine/dictionarySource.ts",
    "what": "only the archive is budgeted, so the disk fills during extraction after the bandwidth is spent",
    "find": "  return archiveBytes + archiveBytes * EXTRACTED_RATIO + MARGIN;",
    "replace": "  return archiveBytes + MARGIN;"
  },
  {
    "id": "dictionary/space-prefers-smallest",
    "file": "src/engine/dictionarySource.ts",
    "what": "the edition order is reversed, so a roomy disk is given the small edition and boot picks a different file than the one it deletes",
    "find": "export const DICTIONARY_EDITIONS = [\"full\", \"core\", \"small\"] as const;",
    "replace": "export const DICTIONARY_EDITIONS = [\"small\", \"core\", \"full\"] as const;"
  },
  {
    "id": "dictionary/offers-unpriced-edition",
    "file": "src/engine/dictionarySource.ts",
    "what": "an edition whose size never resolved is offered anyway, so the disk check is skipped",
    "find": "    if (archive === undefined) continue;",
    "replace": "    if (archive === undefined) return edition;"
  },
  {
    "id": "dictionary/dead-end-offers-retry",
    "file": "src/engine/dictionarySource.ts",
    "what": "every failure offers a retry, including the one where retrying repeats the same two refusals",
    "find": "  return reason !== \"no-source\";",
    "replace": "  return true;"
  },
  {
    "id": "dictionary/filename-drops-the-edition",
    "file": "src/engine/dictionaryLayout.ts",
    "what": "every edition installs to one filename again, so boot opens core's path over small's bytes",
    "find": "  return `system_${edition}.dic`;",
    "replace": "  return \"system_core.dic\";"
  },
  {
    "id": "dictionary/archive-is-not-sweepable",
    "file": "src/engine/dictionaryLayout.ts",
    "what": "a staged archive loses the .part suffix the startup sweeper matches on, stranding ~69 MB per killed download",
    "find": "  return `sudachidict_${edition}-${version}.whl.part`;",
    "replace": "  return `sudachidict_${edition}-${version}.whl`;"
  },
  {
    "id": "dictionary/deletes-the-only-dictionary",
    "file": "src/engine/dictionaryLayout.ts",
    "what": "the edition boot just loaded is also marked deletable, so a machine whose preference never installed loses the one dictionary it had",
    "find": "  return { load: present[0]!, deletable: [] };",
    "replace": "  return { load: present[0]!, deletable: present };"
  },
  {
    "id": "dictionary/listing-trusts-partials",
    "file": "src/engine/dictionaryLayout.ts",
    "what": "a half-written download counts as installed, so the row reports a dictionary the analyzer cannot open",
    "find": "  return DICTIONARY_EDITIONS.find((edition) => dictionaryFileName(edition) === base);",
    "replace": "  return DICTIONARY_EDITIONS.find((edition) => base.startsWith(dictionaryFileName(edition)));"
  },
  {
    "id": "pinyin/modal-ying-ignored",
    "file": "src/engine/pinyin.ts",
    "what": "a lone 应 keeps the single-character reading yìng where the line means the modal yīng",
    "find": "    const pieces = reReadHeteronym(group, read, groups[index - 1], groups[index + 1]);",
    "replace": "    const pieces = read;"
  },
  {
    "id": "pinyin/heteronym-inside-words",
    "file": "src/engine/pinyin.ts",
    "what": "a heteronym is re-read even when the segmenter absorbed it into a word, so 应对 becomes yīng",
    "find": "  if (group.length !== 1) return pieces;",
    "replace": "  if (false) return pieces;"
  },
  {
    "id": "pinyin/modal-ying-in-toneless-row",
    "file": "src/engine/pinyin.ts",
    "what": "the reading is not checked first, so a tone mark is written into a row that has none",
    "find": "  if (pieces[0] !== \"yìng\") return pieces;",
    "replace": "  if (false) return pieces;"
  },
  {
    "id": "pinyin/lone-zhi-ignored",
    "file": "src/engine/pinyin.ts",
    "what": "a lone 只 keeps the classifier reading zhī where the line means the adverb zhǐ",
    "find": "  if (char === \"只\") return readLoneZhiAsOnly(pieces, previous, next);",
    "replace": "  if (char === \"只\") return pieces;"
  },
  {
    "id": "pinyin/lone-zhi-ignores-determiner",
    "file": "src/engine/pinyin.ts",
    "what": "只 after 这/那/一 is read as the adverb, though a classifier needs that determiner",
    "find": "  if (!HAN.test(firstChar(next)) || DETERMINERS.has(preceding)) return pieces;",
    "replace": "  if (!HAN.test(firstChar(next))) return pieces;"
  },
  {
    "id": "pinyin/lone-zhi-without-predicate",
    "file": "src/engine/pinyin.ts",
    "what": "a line-final 只 is read as the adverb, though an adverb needs something to modify",
    "find": "  if (!HAN.test(firstChar(next)) || DETERMINERS.has(preceding)) return pieces;",
    "replace": "  if (DETERMINERS.has(preceding)) return pieces;"
  },
  {
    "id": "pinyin/lone-zhi-in-toneless-row",
    "file": "src/engine/pinyin.ts",
    "what": "the reading is not checked first, so a tone mark is written into a row that has none",
    "find": "  if (pieces[0] !== \"zhī\") return pieces;",
    "replace": "  if (false) return pieces;"
  },
  {
    "id": "pinyin/modal-ying-without-predicate",
    "file": "src/engine/pinyin.ts",
    "what": "a line-final 应 is read as a modal, though a modal needs a predicate after it",
    "find": "  if (!HAN.test(following) || ASPECT_MARKERS.has(following)) return pieces;",
    "replace": "  if (ASPECT_MARKERS.has(following)) return pieces;"
  },
  {
    "id": "pinyin/modal-ying-takes-aspect",
    "file": "src/engine/pinyin.ts",
    "what": "应 before 了/着/过 is read as a modal, though a modal cannot carry aspect",
    "find": "  if (!HAN.test(following) || ASPECT_MARKERS.has(following)) return pieces;",
    "replace": "  if (!HAN.test(following)) return pieces;"
  },
  {
    "id": "pinyin/dict-gaps-unregistered",
    "file": "src/engine/pinyin.ts",
    "what": "words missing from the complete dictionary are not layered in, so 应和 splits",
    "find": "  addDict(DICT_GAPS as Parameters<typeof addDict>[0], { name: \"kashiyomi-gaps\" });",
    "replace": "  void DICT_GAPS;"
  },
  {
    "id": "pinyin/drop-empty-guard",
    "file": "src/engine/pinyin.ts",
    "what": "a blank line is romanized instead of short-circuiting",
    "find": "  if (text.trim() === \"\") return \"\";",
    "replace": "  if (false) return \"\";"
  },
  {
    "id": "pinyin/ignore-join-words",
    "file": "src/engine/pinyin.ts",
    "what": "word grouping is ignored, so every syllable stands alone",
    "find": "    if (options.joinWords) {",
    "replace": "    if (false) {"
  },
  {
    "id": "pinyin/skip-latin-punctuation",
    "file": "src/engine/pinyin.ts",
    "what": "pinyin rows keep CJK punctuation",
    "find": "  return toLatinPunctuation(parts.join(\" \"));",
    "replace": "  return parts.join(\" \");"
  },
  {
    "id": "scriptLang/drop-kana-rung",
    "file": "src/engine/scriptLang.ts",
    "what": "kana no longer decides, so a mixed line falls through to the caller's fallback",
    "find": "  if (hasKana(text)) return \"ja\";",
    "replace": "  if (false) return \"ja\";"
  },
  {
    "id": "scriptLang/japanese-outranks-chinese",
    "file": "src/engine/scriptLang.ts",
    "what": "a japanese glyph form is checked before chinese evidence",
    "find": "  if (hasChineseOnlyGlyphs(text)) return \"zh\";\n  if (hasJapaneseOnlyGlyphs(text)) return \"ja\";",
    "replace": "  if (hasJapaneseOnlyGlyphs(text)) return \"ja\";\n  if (hasChineseOnlyGlyphs(text)) return \"zh\";"
  },
  {
    "id": "scriptLang/han-fallback-ignored",
    "file": "src/engine/scriptLang.ts",
    "what": "ambiguous han text is left untagged instead of taking the caller's evidence",
    "find": "  if (hasHan(text)) return hanFallback;",
    "replace": "  if (hasHan(text)) return undefined;"
  },
  {
    "id": "scriptLang/target-not-trimmed",
    "file": "src/engine/scriptLang.ts",
    "what": "a target language with surrounding whitespace stops matching",
    "find": "  const name = target.trim();",
    "replace": "  const name = target;"
  },
  {
    "id": "scriptLang/font-fallback-abstains",
    "file": "src/engine/scriptLang.ts",
    "what": "a line with no CJK goes untagged, so a Latin-named marker keeps NCM's default font",
    "find": "  return labelScript(text, fallback) ?? fallback;",
    "replace": "  return labelScript(text, fallback);"
  },
  {
    "id": "scriptLang/font-fallback-overrules",
    "file": "src/engine/scriptLang.ts",
    "what": "the document branch overrules the line's own script instead of filling silence",
    "find": "  return labelScript(text, fallback) ?? fallback;",
    "replace": "  return fallback ?? labelScript(text, fallback);"
  },
  {
    "id": "pinyin/no-chengyu-hyphen",
    "file": "src/engine/pinyin.ts",
    "what": "a four-syllable word joins solid instead of taking the GB/T 2+2 hyphen",
    "find": "  if (!allHan || pieces.length !== 4) return pieces.join(\"\");",
    "replace": "  if (true) return pieces.join(\"\");"
  },
  {
    "id": "pinyin/hyphen-at-any-length",
    "file": "src/engine/pinyin.ts",
    "what": "the hyphen is applied to runs that are not four syllables",
    "find": "  if (!allHan || pieces.length !== 4) return pieces.join(\"\");",
    "replace": "  if (!allHan || pieces.length < 4) return pieces.join(\"\");"
  },
  {
    "id": "hanRepair/converts-kyujitai-too",
    "file": "src/engine/hanRepair.ts",
    "what": "repair takes the conversion for every character, modernizing kyujitai as well",
    "find": "    repaired += isSimplifiedForm(original) ? converted[i]! : original;",
    "replace": "    repaired += converted[i]!;"
  },
  {
    "id": "numerals/no-power-euphony",
    "file": "src/engine/numerals.ts",
    "what": "六百 reads ろくひゃく instead of ろっぴゃく",
    "find": "      if (euphony) out += euphony;",
    "replace": "      if (false) out += euphony;"
  },
  {
    "id": "numerals/euphony-on-whole-number",
    "file": "src/engine/numerals.ts",
    "what": "counter assimilation keys on the whole numeral, so 21匹 loses it",
    "find": "  const tail = chars[chars.length - 1]!;",
    "replace": "  const tail = kanji;"
  },
  {
    "id": "numerals/arabic-ignored",
    "file": "src/engine/numerals.ts",
    "what": "arabic digits are not treated as numerals",
    "find": "  return ARABIC.test(text) ? arabicToKanji(text) : \"\";",
    "replace": "  return \"\";"
  },
  {
    "id": "numerals/leading-zero-accepted",
    "file": "src/engine/numerals.ts",
    "what": "an id such as 007 is read as a number",
    "find": "  if (digits.length > 4 || digits[0] === 0) return \"\";",
    "replace": "  if (digits.length > 4) return \"\";"
  },
  {
    "id": "numerals/drop-abstention",
    "file": "src/engine/japanese.ts",
    "what": "the numeral reader overrides readings the analyzer already produced",
    "find": "  if (token.readingKana !== \"\" || !isNumeral(token.surface)) return undefined;",
    "replace": "  if (!isNumeral(token.surface)) return undefined;"
  },
  {
    "id": "jmdict/kana-surfaces-looked-up",
    "file": "src/engine/jmdictReadings.ts",
    "what": "kana-only surfaces are looked up too",
    "find": "    if (!HAN_CHAR.test(token.surface)) return token;",
    "replace": "    if (false) return token;"
  },
  {
    "id": "jmdict/reading-left-as-hiragana",
    "file": "src/engine/jmdictReadings.ts",
    "what": "filled reading is not converted to katakana",
    "find": "    const reading = hiraToKata(found);",
    "replace": "    const reading = found;"
  },
  {
    "id": "kana/hira-to-kata-identity",
    "file": "src/engine/kana.ts",
    "what": "hiraToKata never converts",
    "find": "      ? String.fromCodePoint(code + KATA_TO_HIRA_OFFSET)",
    "replace": "      ? ch"
  },
  {
    "id": "jmdict/overrides-instead-of-filling",
    "file": "src/engine/jmdictReadings.ts",
    "what": "layer overrides an analyzer reading instead of only filling abstentions",
    "find": "    if (token.readingKana === \"\") return { ...token, readingKana: reading };\n\n    return token;",
    "replace": "    return { ...token, readingKana: reading };"
  },
  {
    "id": "japanese/no-word-joining",
    "file": "src/engine/japanese.ts",
    "what": "every analyzer token gets its own romaji word again, so 忘れた is 'wasure ta'",
    "find": "        appendRomaji(romajiParts, romaji, joined || attaches, \"inferred\");",
    "replace": "        appendRomaji(romajiParts, romaji, joined, \"inferred\");"
  },
  {
    "id": "japanese/auxiliary-ignores-previous",
    "file": "src/engine/japanese.ts",
    "what": "an auxiliary joins whatever precedes it, so 学生だった becomes 'gakuseidatta'",
    "find": "    (previousPos === \"動詞\" || previousPos === \"助動詞\" || previousPos === \"形容詞\")",
    "replace": "    true"
  },
  {
    "id": "japanese/copula-joins",
    "file": "src/engine/japanese.ts",
    "what": "です is treated as an inflection, so 学生です becomes 'gakuseidesu'",
    "find": "    token.surface !== \"です\" &&\n",
    "replace": ""
  },
  {
    "id": "japanese/te-form-any-conjunctive",
    "file": "src/engine/japanese.ts",
    "what": "every conjunctive particle joins, so 行くけれど becomes 'ikukeredo'",
    "find": " && TE_FORM.has(token.surface)",
    "replace": ""
  },
  {
    "id": "japanese/drop-prefix-rule",
    "file": "src/engine/japanese.ts",
    "what": "a prefix starts its own word, so お花 becomes 'o hana'",
    "find": "  if (previousPos === \"接頭辞\") return true;\n",
    "replace": ""
  },
  {
    "id": "readingOverrides/no-kana-normalize",
    "file": "src/engine/readingOverrides.ts",
    "what": "a hiragana reading is stored as typed, breaking the katakana token contract",
    "find": "    overrides.set(word, hiraToKata(reading));",
    "replace": "    overrides.set(word, reading);"
  },
  {
    "id": "readingOverrides/accept-empty-reading",
    "file": "src/engine/readingOverrides.ts",
    "what": "an entry with no reading after the = is kept, blanking the word",
    "find": "    if (word === \"\" || reading === \"\") continue;",
    "replace": ""
  },
  {
    "id": "readingOverrides/substring-match",
    "file": "src/engine/readingOverrides.ts",
    "what": "overrides match inside a longer surface, rewriting 春風 within 春風亭",
    "find": "    const reading = overrides.get(token.surface);",
    "replace": "    const reading = [...overrides].find(([w]) => token.surface.includes(w))?.[1];"
  },
  {
    "id": "readingOverrides/fills-only",
    "file": "src/engine/readingOverrides.ts",
    "what": "an override only fills an abstention instead of overriding the analyzer, which is the whole feature",
    "find": "    if (reading === undefined || reading === token.readingKana) return token;",
    "replace": "    if (reading === undefined || token.readingKana !== \"\") return token;"
  },
  {
    "id": "dictstate/preference-follows-the-install",
    "file": "src/engine/dictionaryState.ts",
    "what": "the legacy install field outranks the stored preference, so a failed switch rewrites what the user chose",
    "find": "      asEdition(source.dictPreferredEdition) ?? asEdition(source.dictEdition) ?? \"core\",",
    "replace": "      asEdition(source.dictEdition) ?? asEdition(source.dictPreferredEdition) ?? \"core\","
  },
  {
    "id": "dictstate/empty-legacy-version-stored",
    "file": "src/engine/dictionaryState.ts",
    "what": "an empty legacy version becomes an entry, so a never-installed edition claims a release",
    "find": "  if (typeof legacy === \"string\" && legacy !== \"\" && edition !== undefined) {",
    "replace": "  if (typeof legacy === \"string\" && edition !== undefined) {"
  },
  {
    "id": "dictstate/prune-keeps-absent-editions",
    "file": "src/engine/dictionaryState.ts",
    "what": "versions survive for editions no longer on disk, so the row dates a file nothing can open",
    "find": "    if (!installed.includes(edition)) continue;",
    "replace": "    if (false) continue;"
  },
  {
    "id": "dictstate/switch-pending-on-empty-disk",
    "file": "src/engine/dictionaryState.ts",
    "what": "a machine with no dictionary reports a pending switch instead of a first install",
    "find": "  return inventory.installed.length > 0 && !inventory.installed.includes(preferred);",
    "replace": "  return !inventory.installed.includes(preferred);"
  },
  {
    "id": "rowstate/button-live-during-cooldown",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "the retry button re-enables immediately after a failure, so a double click starts two installs",
    "find": "    primary: { action: { kind: \"retry\" }, disabled: cooling },",
    "replace": "    primary: { action: { kind: \"retry\" }, disabled: false },"
  },
  {
    "id": "rowstate/picker-live-mid-install",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "the edition picker stays enabled while an install runs, so the preference can be rewritten mid-flight",
    "find": "    pickerEnabled: false,",
    "replace": "    pickerEnabled: true,"
  },
  {
    "id": "rowstate/space-quotes-download-size",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "the space warning quotes the archive size instead of the free space an install needs",
    "find": "  const needed = requiredFreeBytes(pinnedRelease(wanted).size);",
    "replace": "  const needed = pinnedRelease(wanted).size;"
  },
  {
    "id": "rowstate/no-source-offers-retry",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "a dead-end failure offers a retry that repeats the same two refused requests",
    "find": "  if (!isRetryable(job.reason)) {",
    "replace": "  if (false) {"
  },
  {
    "id": "rowstate/unknown-space-reads-as-full-disk",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "a disk that could not be measured is treated as having no room, blocking a first install",
    "find": "  if (freeBytes === undefined) return undefined;",
    "replace": "  if (false) return undefined;"
  },
  {
    "id": "rowstate/cancel-live-during-swap",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "cancel stays enabled through the swap and load, offering to stop work that cannot be stopped",
    "find": "        job.phase === \"verifying\" || job.phase === \"extracting\",",
    "replace": "        true,"
  },
  {
    "id": "rowstate/resolve-not-cancellable",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "the update check cannot be cancelled, so a source that hangs leaves the row stuck with a dead button",
    "find": "      return working({ kind: \"checking\" }, \"checking\", true);",
    "replace": "      return working({ kind: \"checking\" }, \"checking\", false);"
  },
  {
    "id": "dictionary/mirror-swap-unguarded",
    "file": "src/engine/dictionarySource.ts",
    "what": "any url is rewritten onto the mirror host, including the vendor archive it does not carry",
    "find": "  return url.startsWith(`${PYTHONHOSTED}/`)",
    "replace": "  return true"
  },
  {
    "id": "dictionary/relay-tried-first",
    "file": "src/engine/dictionarySource.ts",
    "what": "the third-party relay is tried before the publisher and its mirrors",
    "find": "  const urls = [release.url];",
    "replace": "  const urls = [`${RELAY}${github}`, release.url];"
  },
  {
    "id": "dictionary/github-asset-unverified",
    "file": "src/engine/dictionarySource.ts",
    "what": "a github asset with a non-sha256 digest is accepted, so unverifiable bytes install",
    "find": "    if (!digest.startsWith(\"sha256:\")) return undefined;",
    "replace": "    if (false) return undefined;"
  },
  {
    "id": "dictionary/github-tag-keeps-its-v",
    "file": "src/engine/dictionarySource.ts",
    "what": "the release tag keeps its leading v, so every derived filename and version is wrong",
    "find": "  const version = tag.replace(/^v/u, \"\");",
    "replace": "  const version = tag;"
  },
  {
    "id": "dictionary/github-ignores-the-edition",
    "file": "src/engine/dictionarySource.ts",
    "what": "any asset in the release is accepted, so asking for small can install full",
    "find": "    if (asset.name !== wanted) continue;",
    "replace": "    if (false) continue;"
  },
  {
    "id": "dictionary/relay-trusted-for-metadata",
    "file": "src/engine/dictionarySource.ts",
    "what": "the third-party relay is trusted to name a release, so it could supply both the url and the digest it is checked against",
    "find": "    trusted: false,",
    "replace": "    trusted: true,"
  },
  {
    "id": "dictionary/pin-confirmed-on-version-alone",
    "file": "src/engine/dictionarySource.ts",
    "what": "an untrusted source confirms the pin on a matching version alone, so a swapped digest passes",
    "find": "  return release.version === pinned.version && release.sha256 === pinned.sha256;",
    "replace": "  return release.version === pinned.version;"
  },
  {
    "id": "rowstate/switch-labelled-as-update",
    "file": "src/engine/dictionaryRowState.ts",
    "what": "an installed edition that is not the loaded one offers Update, so pressing it reports no change and never switches",
    "find": "      primary: { action: { kind: isSelection ? \"update\" : \"switch\" }, disabled: false },",
    "replace": "      primary: { action: { kind: \"update\" }, disabled: false },"
  }
]
